# Scheduled Release Job Prompt

Work only inside this lab project. Read data/cron-job.json and the approved release package. Select a package only when status is APPROVED, approval_hash equals current master_hash and the idempotency key has no committed video_id. Use the harbour-bean-video skill only if a new render is explicitly requested. Prepare or upload with privacy private, write a receipt and operations ledger, and stop on missing files, auth, rights, approval or duplicate evidence. Never change public visibility.
